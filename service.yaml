apiVersion: v1
kind: Service
metadata:
  name: demo-project-service
spec:
  type: NodePort
  selector:
    app: demo-project
  ports:
    - protocol: TCP
      port: 7000
      targetPort: 7000
      nodePort: 30007